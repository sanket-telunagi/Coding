import math
if __name__ == "__main__" :

    # t = int(input()) 
    # while (t) :

    #     x = int(input())
    #     print(math.ceil(x**0.5))
    #     t -= 1
    for i in range(int(input())) : print(math.ceil(int(input())**0.5)) 
    # The idea for using ceiling function with taking suareroot strikes by accident 
    # thsi may be because the numbers were too close to square root of the input 
    # observation is the key 